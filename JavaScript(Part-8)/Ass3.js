//Create a new array whose elements are in uppercase of words present in the original array.?

let originalArr = ["jagrati","rathore"];
let newArr = originalArr.map(word=>word.toUpperCase());
console.log(newArr);
